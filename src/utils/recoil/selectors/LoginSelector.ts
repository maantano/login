import { selector } from "recoil";
import { LoginState } from "../atoms/LoginState";
// import { todoState } from "../atoms/todoState";

// export const loginSortState = selector({
//   key: "loginSortState",
//   get: ({ get }) => {
//     const data = get(LoginState);
//     // 서버에서 id 리스트 받아와서 loginState의 데이터와 같은것을 반환 하는것으로 만들어야됨
//     const result = data.filter((v: any) => v.state === true);
//     return result;
//   },
// });
